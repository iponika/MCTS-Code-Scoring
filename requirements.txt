# Core Python dependencies for the code-scoring MCTS adaptation.
#
# Install a CUDA-compatible PyTorch build first if your cluster requires a
# specific wheel/index. vLLM may also pin a compatible torch version.
accelerate>=1.0.0
datasets>=2.21.0
jsonlines>=4.0.0
numpy>=1.26.0
omegaconf>=2.3.0
openai>=1.58.0
pandas>=2.2.0
partial-json-parser>=0.2.1
Pebble>=5.1.0
peft>=0.13.0
pydantic>=2.10.0
pyarrow>=18.0.0
PyYAML>=6.0.0
pytest>=8.3.0
requests>=2.32.0
safetensors>=0.4.5
sentencepiece>=0.2.0
termcolor>=2.5.0
timeout-decorator>=0.5.0
tqdm>=4.67.0
trl>=0.15.2
vllm>=0.8.0

# Qwen3.5 support may require a newer Transformers build than some package
# indexes provide. Keep this line unless the target server already has a
# release that recognizes model_type=qwen3_5.
transformers @ git+https://github.com/huggingface/transformers.git
